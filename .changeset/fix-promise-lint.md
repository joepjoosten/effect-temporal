---
"@effect-temporal/workflow": patch
---
Allow AbortSignal parameters in promise thunks. Replace the recommended zero-arity rule with effect-promise-arity while retaining the old name as a compatible alias.
