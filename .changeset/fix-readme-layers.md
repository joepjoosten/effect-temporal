---
"@effect-temporal/workflow": patch
---
Fix the quick-start worker and client layer composition so dependencies are provided correctly. Typecheck the published quick-start snippets during validation.
